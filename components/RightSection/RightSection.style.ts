import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    longSpace: {
        marginTop: 150,
        marginRight: 30,
    },
    space: {
        marginBottom: 20,
    },
    rightContainer: {
        position: "absolute",
        right: 30,
    },
});
