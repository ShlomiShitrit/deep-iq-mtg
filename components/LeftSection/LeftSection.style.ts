import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    leftContainer: {
        position: "absolute",
        left: 30, // Adjust as needed
    },
    space: {
        marginBottom: 20,
    },
});
