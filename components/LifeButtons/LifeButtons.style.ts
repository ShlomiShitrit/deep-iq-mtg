import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    buttonText: {
        fontSize: 30,
        fontWeight: "bold",
    },
    leftButton: {
        position: "absolute",
        left: 0,
        marginLeft: 10,
    },
    rightButton: {
        position: "absolute",
        right: 0,
        marginRight: 10,
    },
    lifeText: {
        fontSize: 30,
        fontWeight: "bold",
        zIndex: 1,
    },
});
